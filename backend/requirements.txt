# ─── Web Framework ───────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ─── Database ────────────────────────────────────────────────────
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
pgvector==0.2.4

# ─── Cache ───────────────────────────────────────────────────────
redis==5.0.1

# ─── Auth & Security ─────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.25.2

# ─── Data Validation ─────────────────────────────────────────────
pydantic==2.5.2
pydantic-settings==2.1.0

# ─── Audio ML ────────────────────────────────────────────────────
librosa==0.10.1
numpy==1.26.2
scipy==1.11.4
scikit-learn==1.3.2

# ─── Dev & Testing ───────────────────────────────────────────────
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
ruff==0.1.6

# NOTE: torch, mlflow, and apache-airflow excluded intentionally.
# - torch: install separately when building the ML module
# - mlflow: runs in its own container (see docker-compose.yml)
# - apache-airflow: runs in its own container (requires sqlalchemy<2.0, conflicts here)

# ─── Web Framework ───────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.32

# ─── Database ────────────────────────────────────────────────────
sqlalchemy==2.0.23
alembic==1.19.1
psycopg2-binary==2.9.9
pgvector==0.5.0

# ─── Cache ───────────────────────────────────────────────────────
redis==5.0.1

# ─── Auth & Security ─────────────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
httpx==0.25.2

# ─── Data Validation ─────────────────────────────────────────────
pydantic==2.5.2
pydantic-settings==2.1.0

# ─── Audio ML ────────────────────────────────────────────────────
# librosa: feature extraction (MFCCs, chroma, spectral, tempo)
librosa==0.10.1
numpy==1.26.2
scipy==1.18.1
scikit-learn==1.9.0
# yt-dlp: sources audio from YouTube for tracks missing Spotify previews.
# Spotify Audio Features API (/audio-features) is blocked in Development Mode
# and deprecated for new apps. yt-dlp + librosa replaces it with richer,
# market-agnostic features that work for Telugu, Tamil, Hindi, and all global catalogs.
yt-dlp>=2024.12.23

# ─── Dev & Testing ───────────────────────────────────────────────
# pytest and pytest-asyncio move together: pytest-asyncio caps the pytest it
# accepts, and 1.3.0 is the first release to widen that cap to `<10`, so
# pytest 9 requires pytest-asyncio >= 1.3. pytest-cov declares `pytest>=7`
# with no upper bound and needs no bump.
# Loop-scope configuration lives in pytest.ini (asyncio_mode +
# asyncio_default_{fixture,test}_loop_scope); both keys are still honoured by
# pytest-asyncio 1.x.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==6.3.0
ruff==0.15.8

# NOTE: torch, mlflow, and apache-airflow excluded intentionally.
# - torch: install separately when building the ML module (Phase 3)
# - mlflow: runs in its own container (see docker-compose.yml)
# - apache-airflow: runs in its own container (requires sqlalchemy<2.0, conflicts here)
